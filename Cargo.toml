[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.9.0"
dotenv = "0.15.0"
serde = { version = "1.0.208", features = ["derive"] }
sqlx = { version = "0.8.2", features = ["sqlite", "runtime-async-std"] }
